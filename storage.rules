rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated org staff can read or write any file
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
